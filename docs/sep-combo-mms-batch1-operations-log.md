# 9월 콤보 전 메뉴 MMS — 첫 배치 운영 기록

## 2026-09-14 KST 예약 전 검증

| 검증 항목 | 확인값 |
|---|---|
| 대상 파일 | `nops_sep_combo_2026_batch_1_900recipients.xlsx` |
| 생성 시각 | 2026-09-14 14:42:37 KST |
| 수신자 | 900명 |
| 배치 SHA-256 | `7322b384e3a799204704eb7b030a27fbe2c46cd4409b2e053b307984380bb859` |
| 전체 자동 발송 대상 | 2,723명, 회원 ID·정규화 전화번호 모두 중복 없음 |
| 제외 처리 | 테스트 수신 3명, 사용 완료 2명, 발송 불가 전화번호 11명, 동일 전화번호 5명 — 합계 21건 |
| 콘솔 잔액 | 353,993.6원 |
| 콘솔 일일 한도 | 996건 |
| 발신번호 | 02-597-4030 |
| 이미지 | `KakaoTalk_20260914_141131266_01.jpg`, JPG 199KB |
| 광고 서비스명 | 스테이크하우스NOPS |

## 확정 메시지 구성

제목은 `(광고)[NOPS] 쿠폰 발급!`으로 유지한다. 본문은 이름 치환, 마이페이지, 2026.10.11 사용 기한, 콤보 전 메뉴 적용 범위, 카카오 메뉴 링크, 문의 번호만 포함한다. SOLAPI 광고성 설정이 080 수신거부 정보를 자동 삽입하므로 본문에는 080 문구를 수동 중복 입력하지 않는다.

## 콘솔 조치 상태

기존 테스트 수신자 2명은 콘솔 수신자 명단에서 제거했다. 본문은 이전의 `베네핏 스테이크 출시기념` 변형 문구에서 확정 문구로 정정했다. 아직 첫 900명 XLSX 업로드와 예약 등록은 완료되지 않았다.

## 예약 보류 상태

2026-09-14 KST에 예약 브라우저 연결이 중단됐다. 전환된 별도 세션은 발신번호가 미선택이고, 잔액이 54,477.6원으로 표시되어 첫 배치 예상 비용을 충족하지 못했다. 이는 직전 예약 세션에서 확인한 353,993.6원과 불일치한다. 잘못된 세션·발신번호로 예약될 위험이 있어, 실제 충전 계정의 브라우저 세션을 복구하고 잔액·발신번호를 재확인하기 전에는 예약을 등록하지 않는다.

## 즉시 발송 전환 및 접수 결과

2026-09-14 15:44 KST에 15:00 KST 예약 시각 경과에 따라 첫 배치를 즉시 발송으로 전환했다. API 발신번호를 콘솔 승인 발신번호 `02-597-4030`으로 정정하고, SOLAPI 잔액 조회 테스트를 통과한 뒤 실행했다. 테스트 수신이 성공했던 이미지 ID와 확정 제목·본문·080 수신거부 문구를 사용했다. SOLAPI 그룹 ID는 `G4V20260914154430IFRKVPESXIHBWVS`이며, 900건 등록 성공·등록 실패 0건으로 접수됐다. 2026-09-14 15:49 KST 초기 상태는 전송 성공 873건, 이동통신사 처리 대기 25건, 전송 실패 2건(환불 2건), 그룹 상태 `SENDING`이다. 실패 대상의 번호·이름은 운영 기록에 저장하지 않으며, 그룹 완료 후 오류 코드만 별도 집계한다.

## 개인화 오류 분석 및 후속 조치

2026-09-14 17:54 KST SOLAPI 발송 이력 900건을 번호·이름 없이 감사한 결과, `#{이름}` 변수가 남은 건은 900건 전부였다. 메시지 생성 코드가 문자 본문 변수 치환을 지원하지 않는 `customFields.recipientName`에 이름을 넣었고, 본문에는 공통 문자열 `#{이름}`을 그대로 보낸 것이 원인이다. 내보낸 원본 명단은 모든 행에 이름이 있어 대상 데이터 누락 문제가 아니다.

첫 배치는 변경할 수 없으므로, 900명 전체에 이름 없는 쿠폰 안내가 이미 전달된 상태다. 쿠폰명·마이페이지 링크·사용기한·메뉴 링크·문의·080 수신거부는 본문에 정상 포함됐고, 이름만 치환되지 않았다. 발송 결과는 수신 완료 875건, 통신사 처리 중 23건, 실패 2건이다. 실패 코드는 미가입자 1건(`3032`)과 기타 단말기 문제 1건(`3046`)이며, 두 건은 SOLAPI 환불로 표시됐다.

남은 배치에는 각 수신자 이름을 메시지 본문에 직접 삽입하도록 변경했다. 2차 900명과 3차 923명 전체에서 미치환 변수 0건, 수신거부 문구 100% 포함, 전화번호 중복 0건을 사전검증했다. 1차 오류를 보정하는 재발송은 동일 안내의 중복 수신과 추가 비용을 발생시키므로, 별도 승인 전에는 하지 않는다.

## 2·3차 통합 예약 결과

사용자 승인에 따라 2차 900명과 3차 923명을 하나로 합쳐 2026-09-15 15:00:00 KST에 예약했다. SOLAPI 그룹 ID는 `G4V20260914181238EBYTH8ERBLDIC0P`이다. 예약 상태는 `SCHEDULED`, 등록 성공 1,823건, 등록 실패 0건이며, SOLAPI 반환 예약 시각은 `2026-09-15T06:00:00.000Z`로 KST 15:00과 일치한다. 직접 본문 삽입으로 1,823건 모두 이름 치환 변수 0건을 확인했고, 발신번호는 `02-597-4030`, 이미지 ID는 테스트 수신이 성공한 기존 MMS 이미지로 고정했다.

## 최채환 개인화 테스트 결과

최채환 1명은 SMS 마케팅 동의·활성 이벤트 쿠폰·활성 회원 조건을 충족했고, 내일 1,823명 예약 명단에는 포함되지 않은 것을 확인한 뒤 테스트 발송했다. SOLAPI 그룹 ID는 `G4V20260914195705LYVST4THBU7E2NQ`이다. 등록 성공 1건·등록 실패 0건이며, 그룹 상태는 `COMPLETE`, 전송 성공 1건·실패 0건으로 확인됐다. 이 테스트는 이름을 `#{이름}` 변수로 보내지 않고 본문에 직접 삽입한 동일한 개인화 방식이다. 단말에서 이름·이미지·링크가 실제로 표시되는 최종 확인은 수신자 확인 대기다.
